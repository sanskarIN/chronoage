# ChronoAge 2.1.0 — Workstream Status

Status date: 2026-09-02

This status page records the next-version work started after the `2.0.13` release-evidence hardening cycle.

## Completed in this continuation

- Added the `2.1.0` engineering plan with explicit reproducibility, CI, governance, backup-compatibility, internationalization, and release-evidence gates.
- Added a dedicated `2.1.0` section to the roadmap without falsely marking evidence-gated work complete.
- Added an Unreleased changelog section documenting the planned engineering sequence.
- Added a dependency-install policy preflight that distinguishes the current pre-lockfile state from the required post-lockfile `npm ci` state.
- Wired the policy preflight into permanent CI, Native CI, Release verification, and the normal `npm run check` chain.
- Recorded the policy as ADR 0009 so the eventual lockfile migration has an explicit, reviewable contract.

## Verified repository facts

- Public repository: `sanskarIN/chronoage`.
- Current source version remains `2.0.13`.
- Default branch remains `main`.
- Node release/runtime pin remains `22.13.0`.
- Rust toolchain pin remains `1.97.1`.
- The release workflow already uses lockfile preflight and `npm ci`.
- The permanent CI and Native CI workflows still use `npm install` while the genuine npm lockfile is absent; the new policy check will automatically reject that state after `package-lock.json` is committed.
- `package-lock.json` and `src-tauri/Cargo.lock` are not present on the current `main` tree and must be generated by real registry resolution rather than authored manually.
- The GitHub repository API reports `main` as unprotected; branch governance therefore remains an external repository-setting task.

## Next execution order

1. Obtain a network-enabled clean checkout with Node `22.13.0` and generate the genuine npm lockfile.
2. Review and verify the npm dependency graph; run the complete web quality/E2E gates.
3. Commit only the reviewed npm lockfile; the install-policy preflight will then require permanent CI/Native CI to use `npm ci`.
4. Generate and review the genuine Cargo lockfile with Rust `1.97.1`.
5. Enforce locked native verification and run the full hosted native matrix.
6. Configure and verify `main` protection/ruleset.
7. Implement the first reviewed additional locale pack.
8. Run final 2.1.0 release evidence checks.

## Evidence policy

No dependency lockfile, CI result, native artifact, signing result, store publication, branch-protection state, or release tag is considered complete merely because it is planned or documented. Each gate must be backed by direct repository or hosted-run evidence.
